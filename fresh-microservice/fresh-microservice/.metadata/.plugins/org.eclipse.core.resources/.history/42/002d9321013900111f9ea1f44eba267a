server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/booknest_auth_db?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: YOUR_GOOGLE_CLIENT_ID
            client-secret: YOUR_GOOGLE_CLIENT_SECRET
            scope: email, profile
          github:
            client-id: Ov23liyItzcbh0vLCbnc
            client-secret: f25ddbd6af980d53d1c6bb5ca8c658e6a05db1f3
            scope: user:email, read:user

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/